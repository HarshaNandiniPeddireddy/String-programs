#include<bits/stdc++.h>
using namespace std;
int main()
{
    int c=1,j=0;
    string str,a;
    getline(cin,str);
    for(int i=0;i<str.length();i++){
        if(str[i]==str[i+1])
        {
            c++;
        }
        else
        {
            cout<<str[i]<<c;c=1;
        }
    }
}