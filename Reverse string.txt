#include <iostream>
#include<string>
using namespace std;
int main() {
   int L=0;
   string s;
   getline(cin,s);
   L=s.length();
   for(int i=L-1;i>=0;i--)
     cout<<s[i];
     
    return 0;
}